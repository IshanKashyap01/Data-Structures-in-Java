# Merge Sort

It is a recursive algorithm and is much faster than insertion, selection and
bubble sort

## Algorithm

1. Divide the array into two halves

2. Assuming the function can sort a smaller array, call merge sort on both

3. Merge the two sorted arrays back into the original array
